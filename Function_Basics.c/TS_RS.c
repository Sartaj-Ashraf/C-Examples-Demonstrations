#include<stdio.h>
void multi(int a, int b);
int main()
{ 
  int product;
  printf("Eter the value of your numers");
  scanf("%d %d", a,b);
  product=multi(c);
  printf("The product of these two numebrs is as  %d",c);

}
void multi(int a, int b)
{
    int c;
    c=a*b;
    return(c);
}