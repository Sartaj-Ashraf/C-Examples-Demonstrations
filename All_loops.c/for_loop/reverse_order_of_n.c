#include <stdio.h>

int main()
{
    int n;
    printf("enter the value of n ");
    scanf("%d" , &n);

    for (int i = n; i; i--)
    {
        printf("the value of n is  %d\n", i=i-1   ); /*(here is how we can count it in even or odd numbers));*/
    }
}