#include<stdio.h>
#include<stdlib.h>
#include<string.h>

int main()
{
    int arr[10];
    int num;
    int max[i];
    max=max[i];
    printf("Enter size of array :: ");
    scanf("%d",&num);
    int max[i];
            int min;
            max[i] = arr[0];
            for (i = 0; i < num; i++)
            {

                if (arr[i] > max)
                {
                    max[i] = arr[i];

                }
                printf("%d",max[i])
            }


}