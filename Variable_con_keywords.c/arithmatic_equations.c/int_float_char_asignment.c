#include <stdio.h>
int main()
{
  int a = 1.4;
  // %d is for iteger values
  printf("the value of a is : %d\n", a);  
  // also %d is the value that you asign to the character
  // also int =2.1 is not valid ,because 2.1 is an float value

  float b = 2.1;  
  // %f is for float values. decimal values.
  printf("the value of b is : %f\n", b);
 
  int z = 3;
  printf("the value of z is : %d\n", z);

  char sartaj = 's';
  // %c is for character values. (must be in single inverted commas)
  printf("the value of sartaj  is : %c\n", sartaj);gue
}


